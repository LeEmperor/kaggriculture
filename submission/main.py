"""Generated Kaggriculture agent.  Do not edit; rebuild with
``python3 -m tools.build_submission`` from the project repository.
"""

from __future__ import annotations

import json as _json
import sys as _sys
import types as _types

POLICY_ID = 'monocrop-reorder-v1'
POLICY_FAMILY = 'monocrop_reorder'
POLICY_FAMILY_VERSION = 1
CANDIDATE_SCHEMA_VERSION = 1
POLICY_PARAMETERS = _json.loads('{"$schema":"./parameters.schema.json","policy_id":"monocrop-reorder-v1","schema_version":1,"parameters":{"crop":"WHEAT","cash_reserve":250,"seed_reorder_point":0,"seed_buy_batch":4,"planting_hour_cutoff":20,"harvest_min_age_days":4,"sell_price_threshold":25,"liquidation_start_day":27}}')["parameters"]
BUILD_INPUT_SHA256 = {
    "family": '4cd3af79e5a6fdc2736d2ba43ff705110bd3de47562317dd7528954b37109645',
    "candidate": '07087c98c6d9019219c8d021fe120665681040e847ac2b7e53feebeb40ae0970',
    "runtime": '6e8680ef9d2b93a21fd2b199cd6c75ed0d2b04048eec58930fe40080d0124b2d',
    "builder": '685dff5b0337f5cf2d8c26301982049358434ef70b4e1d53a2049436ba73c474',
}

_RUNTIME_PREFIX = '_kaggriculture_submission'
_ARTIFACT_FILE = globals().get("__file__", "<kaggriculture-main.py>")
_MODULE_SOURCES = {
    'dsl.expr': '"""Role 1 of the policy DSL: the expression language.\n\nPrefix-JSON expressions (``["+", ["obs", "day"], ["const", 1]]``) are parsed\ninto a small immutable AST, kind-checked against a declared type environment,\nand evaluated against a per-stage environment.\n\nThis module knows about integers, booleans, and enum strings. It knows nothing\nabout farming, observations, registers, or stages beyond the names it is handed,\nand it imports nothing from this project. That is deliberate: it is seam 2 of\n``docs/library_boundaries.md``, the piece that is domain-free once the\nobservation vocabulary is cut out.\n"""\n\nfrom __future__ import annotations\n\nfrom collections.abc import Callable, Mapping\nfrom dataclasses import dataclass, field\nfrom typing import Any, Literal\n\n# A DSL value. ``bool`` is checked before ``int`` everywhere, because Python\n# makes True an int and the language does not.\nValue = int | bool | str\n\nStage = Literal["reset", "observe", "decide", "commit"]\n\n\nclass DslError(ValueError):\n    """A malformed or ill-typed encoding, reported with the path that failed."""\n\n    def __init__(self, path: str, message: str) -> None:\n        super().__init__(f"{path}: {message}")\n        self.path = path\n\n\n@dataclass(frozen=True)\nclass Kind:\n    """The static type of an expression.\n\n    ``values`` narrows a ``str`` to an enum domain. Comparing two disjoint\n    domains is rejected at parse time, which is what catches a misspelled state\n    name such as ``"LIQUIDATON"`` before it silently evaluates to False for an\n    entire episode.\n    """\n\n    base: Literal["int", "bool", "str"]\n    values: frozenset[str] | None = None\n\n    def __str__(self) -> str:\n        if self.values is None:\n            return self.base\n        return f"{self.base}{{{\', \'.join(sorted(self.values))}}}"\n\n\nINT = Kind("int")\nBOOL = Kind("bool")\nSTR = Kind("str")\n\n\n# --------------------------------------------------------------------------\n# AST\n# --------------------------------------------------------------------------\n\n\n@dataclass(frozen=True)\nclass Const:\n    value: Value\n\n\n@dataclass(frozen=True)\nclass Param:\n    name: str\n\n\n@dataclass(frozen=True)\nclass State:\n    """A register as of the start of the current stage."""\n\n    name: str\n\n\n@dataclass(frozen=True)\nclass Next:\n    """A register as written earlier in this stage\'s declaration order."""\n\n    name: str\n\n\n@dataclass(frozen=True)\nclass Obs:\n    name: str\n\n\n@dataclass(frozen=True)\nclass Fired:\n    """The name of the rule that fired in a first-match-wins cascade."""\n\n    group: str\n\n\n@dataclass(frozen=True)\nclass FiredAny:\n    """Whether a named rule fired in an all-match rule group."""\n\n    group: str\n    rule: str\n\n\n@dataclass(frozen=True)\nclass Op:\n    op: str\n    args: tuple[Expr, ...]\n\n\nExpr = Const | Param | State | Next | Obs | Fired | FiredAny | Op\n\n\n# --------------------------------------------------------------------------\n# Parsing\n# --------------------------------------------------------------------------\n\n_ARITHMETIC = ("+", "-", "*", "min", "max")\n_ORDERING = ("<", "<=", ">", ">=")\n_EQUALITY = ("==", "!=")\n_NARY_LOGIC = ("and", "or")\n\n_FIXED_ARITY: Mapping[str, int] = {\n    **dict.fromkeys(_ARITHMETIC, 2),\n    **dict.fromkeys(_ORDERING, 2),\n    **dict.fromkeys(_EQUALITY, 2),\n    "not": 1,\n    "if": 3,\n}\n\nOPERATORS: frozenset[str] = frozenset(_FIXED_ARITY) | frozenset(_NARY_LOGIC)\n\n_LEAF_ARITY: Mapping[str, int] = {\n    "const": 1,\n    "param": 1,\n    "state": 1,\n    "next": 1,\n    "obs": 1,\n    "fired": 1,\n    "fired?": 2,\n}\n\n\ndef parse(node: Any, path: str = "expr") -> Expr:\n    """Parse one prefix-JSON expression into the AST.\n\n    Rejects floats and null outright. ``docs/policy_dsl.md`` writes\n    ``["const", null]`` and ``["const", 0.0]`` in the worked encoding, but the\n    leaf specification in that same document admits only int, bool, and string.\n    The document is wrong rather than this parser: see the ``money_seen``\n    register in ``family.json`` for how the family avoids needing either.\n    """\n    if not isinstance(node, list) or not node:\n        raise DslError(path, f"expected a non-empty JSON array, got {node!r}")\n    head = node[0]\n    if not isinstance(head, str):\n        raise DslError(path, f"expression head must be a string, got {head!r}")\n    operands = node[1:]\n\n    if head in _LEAF_ARITY:\n        _check_arity(head, operands, _LEAF_ARITY[head], path)\n        return _parse_leaf(head, operands, path)\n\n    if head in _NARY_LOGIC:\n        if len(operands) < 1:\n            raise DslError(path, f"\'{head}\' needs at least one operand")\n        return Op(head, tuple(_parse_args(operands, path, head)))\n\n    if head in _FIXED_ARITY:\n        _check_arity(head, operands, _FIXED_ARITY[head], path)\n        return Op(head, tuple(_parse_args(operands, path, head)))\n\n    raise DslError(path, f"unknown expression head \'{head}\'")\n\n\ndef _parse_args(operands: list[Any], path: str, head: str) -> list[Expr]:\n    return [parse(arg, f"{path}.{head}[{i}]") for i, arg in enumerate(operands)]\n\n\ndef _check_arity(head: str, operands: list[Any], want: int, path: str) -> None:\n    if len(operands) != want:\n        raise DslError(\n            path, f"\'{head}\' takes {want} operand(s), got {len(operands)}"\n        )\n\n\ndef _parse_leaf(head: str, operands: list[Any], path: str) -> Expr:\n    if head == "const":\n        value = operands[0]\n        # bool is a subclass of int, so this admits all three leaf types\n        # and rejects float and None.\n        if isinstance(value, (int, str)):\n            return Const(value)\n        raise DslError(\n            path,\n            f"const must be an int, bool, or string; got {value!r}. The DSL has "\n            "no float or null literal.",\n        )\n    name = operands[0]\n    if not isinstance(name, str):\n        raise DslError(path, f"\'{head}\' takes a string name, got {name!r}")\n    if head == "param":\n        return Param(name)\n    if head == "state":\n        return State(name)\n    if head == "next":\n        return Next(name)\n    if head == "obs":\n        return Obs(name)\n    if head == "fired":\n        return Fired(name)\n    rule = operands[1]\n    if not isinstance(rule, str):\n        raise DslError(path, f"\'fired?\' takes a string rule name, got {rule!r}")\n    return FiredAny(name, rule)\n\n\n# --------------------------------------------------------------------------\n# Kind inference\n# --------------------------------------------------------------------------\n\n\n@dataclass(frozen=True)\nclass TypeEnv:\n    """Everything ``infer`` needs to give an expression a kind.\n\n    ``stage`` and ``next_available`` carry the stage restrictions from\n    ``docs/policy_dsl.md``: ``["next", r]`` is legal only in ``observe`` and\n    ``commit`` and only for a register written earlier in that same stage, and\n    the ``fired`` leaves are legal only in ``commit``.\n    """\n\n    params: Mapping[str, Kind]\n    registers: Mapping[str, Kind]\n    observations: Mapping[str, Kind]\n    stage: Stage\n    next_available: frozenset[str] = frozenset()\n    groups: Mapping[str, frozenset[str]] = field(default_factory=dict)\n\n\ndef infer(expr: Expr, env: TypeEnv, path: str = "expr") -> Kind:\n    """Return the static kind of ``expr``, raising ``DslError`` if ill-typed."""\n    match expr:\n        case Const(value):\n            if isinstance(value, bool):\n                return BOOL\n            if isinstance(value, int):\n                return INT\n            return Kind("str", frozenset({value}))\n\n        case Param(name):\n            return _lookup(env.params, name, "parameter", path)\n\n        case State(name):\n            return _lookup(env.registers, name, "register", path)\n\n        case Next(name):\n            if env.stage not in ("observe", "commit"):\n                raise DslError(\n                    path, f"[\'next\', ...] is not allowed in the {env.stage} stage"\n                )\n            if name not in env.next_available:\n                raise DslError(\n                    path,\n                    f"[\'next\', \'{name}\'] must name a register written earlier in "\n                    "this stage",\n                )\n            return _lookup(env.registers, name, "register", path)\n\n        case Obs(name):\n            return _lookup(env.observations, name, "observation", path)\n\n        case Fired(group):\n            _check_commit_only(env, path, "[\'fired\', ...]")\n            rules = _lookup_group(env.groups, group, path)\n            # The empty string is what a group with no matching rule yields.\n            return Kind("str", rules | {""})\n\n        case FiredAny(group, rule):\n            _check_commit_only(env, path, "[\'fired?\', ...]")\n            rules = _lookup_group(env.groups, group, path)\n            if rule not in rules:\n                raise DslError(path, f"group \'{group}\' has no rule named \'{rule}\'")\n            return BOOL\n\n        case Op(op, args):\n            return _infer_op(op, args, env, path)\n\n    raise DslError(path, f"unhandled expression {expr!r}")\n\n\ndef _infer_op(op: str, args: tuple[Expr, ...], env: TypeEnv, path: str) -> Kind:\n    kinds = [\n        infer(arg, env, f"{path}.{op}[{i}]") for i, arg in enumerate(args)\n    ]\n\n    if op in _ARITHMETIC:\n        _require_all(kinds, "int", op, path)\n        return INT\n    if op in _ORDERING:\n        _require_all(kinds, "int", op, path)\n        return BOOL\n    if op in _NARY_LOGIC:\n        _require_all(kinds, "bool", op, path)\n        return BOOL\n    if op == "not":\n        _require_all(kinds, "bool", op, path)\n        return BOOL\n    if op in _EQUALITY:\n        left, right = kinds\n        if left.base != right.base:\n            raise DslError(\n                path, f"\'{op}\' compares {left} with {right}; kinds must match"\n            )\n        if (\n            left.base == "str"\n            and left.values is not None\n            and right.values is not None\n            and not (left.values & right.values)\n        ):\n            raise DslError(\n                path,\n                f"\'{op}\' compares disjoint enum domains {left} and {right}; "\n                "this comparison can never be true",\n            )\n        return BOOL\n    if op == "if":\n        cond, then, other = kinds\n        if cond.base != "bool":\n            raise DslError(path, f"\'if\' condition must be bool, got {cond}")\n        if then.base != other.base:\n            raise DslError(\n                path, f"\'if\' branches disagree: {then} versus {other}"\n            )\n        return _join(then, other)\n\n    raise DslError(path, f"unknown operator \'{op}\'")\n\n\ndef _join(left: Kind, right: Kind) -> Kind:\n    if left.values is None or right.values is None:\n        return Kind(left.base)\n    return Kind(left.base, left.values | right.values)\n\n\ndef _require_all(kinds: list[Kind], base: str, op: str, path: str) -> None:\n    for i, kind in enumerate(kinds):\n        if kind.base != base:\n            raise DslError(\n                path, f"\'{op}\' operand {i} must be {base}, got {kind}"\n            )\n\n\ndef _lookup(table: Mapping[str, Kind], name: str, what: str, path: str) -> Kind:\n    try:\n        return table[name]\n    except KeyError:\n        known = ", ".join(sorted(table)) or "<none>"\n        raise DslError(\n            path, f"unknown {what} \'{name}\'. Declared: {known}"\n        ) from None\n\n\ndef _lookup_group(\n    groups: Mapping[str, frozenset[str]], group: str, path: str\n) -> frozenset[str]:\n    try:\n        return groups[group]\n    except KeyError:\n        known = ", ".join(sorted(groups)) or "<none>"\n        raise DslError(\n            path, f"unknown rule group \'{group}\'. Declared: {known}"\n        ) from None\n\n\ndef _check_commit_only(env: TypeEnv, path: str, what: str) -> None:\n    if env.stage != "commit":\n        raise DslError(\n            path, f"{what} is only available in the commit stage, not {env.stage}"\n        )\n\n\n# --------------------------------------------------------------------------\n# Evaluation\n# --------------------------------------------------------------------------\n\n\n@dataclass(frozen=True)\nclass Env:\n    """Per-stage evaluation environment.\n\n    ``state`` is the register snapshot taken at the start of the stage, and\n    ``next`` holds only the registers already written within it. Keeping them\n    separate is what makes simultaneous commit unambiguous: an expression can\n    only see an in-stage write by asking for it explicitly.\n    """\n\n    params: Mapping[str, Value]\n    state: Mapping[str, Value]\n    observe: Callable[[str], Value]\n    next: Mapping[str, Value] = field(default_factory=dict)\n    groups: Mapping[str, tuple[str, ...]] = field(default_factory=dict)\n\n\ndef evaluate(expr: Expr, env: Env) -> Value:\n    """Evaluate a kind-checked expression.\n\n    Assumes ``infer`` has already accepted the expression against a matching\n    ``TypeEnv``; it performs no type checking of its own.\n    """\n    match expr:\n        case Const(value):\n            return value\n        case Param(name):\n            return env.params[name]\n        case State(name):\n            return env.state[name]\n        case Next(name):\n            return env.next[name]\n        case Obs(name):\n            return env.observe(name)\n        case Fired(group):\n            fired = env.groups.get(group, ())\n            return fired[0] if fired else ""\n        case FiredAny(group, rule):\n            return rule in env.groups.get(group, ())\n        case Op(op, args):\n            return _evaluate_op(op, args, env)\n    raise DslError("eval", f"unhandled expression {expr!r}")\n\n\ndef _evaluate_op(op: str, args: tuple[Expr, ...], env: Env) -> Value:\n    # Short-circuit forms are evaluated before anything else touches operands,\n    # because guards such as harvest_ready rely on `and` to keep an accessor\n    # from being asked for a field the tile does not have.\n    if op == "and":\n        for arg in args:\n            if not evaluate(arg, env):\n                return False\n        return True\n    if op == "or":\n        for arg in args:\n            if evaluate(arg, env):\n                return True\n        return False\n    if op == "if":\n        cond, then, other = args\n        return evaluate(then if evaluate(cond, env) else other, env)\n    if op == "not":\n        return not evaluate(args[0], env)\n\n    values = [evaluate(arg, env) for arg in args]\n    if op == "+":\n        return values[0] + values[1]\n    if op == "-":\n        return values[0] - values[1]\n    if op == "*":\n        return values[0] * values[1]\n    if op == "min":\n        return min(values[0], values[1])\n    if op == "max":\n        return max(values[0], values[1])\n    if op == "<":\n        return values[0] < values[1]\n    if op == "<=":\n        return values[0] <= values[1]\n    if op == ">":\n        return values[0] > values[1]\n    if op == ">=":\n        return values[0] >= values[1]\n    if op == "==":\n        return values[0] == values[1]\n    if op == "!=":\n        return values[0] != values[1]\n    raise DslError("eval", f"unknown operator \'{op}\'")\n',
    'dsl.cascade': '"""Role 2 of the policy DSL: rule groups.\n\nTwo selection disciplines, both over the same ``Rule`` shape:\n\n``select_first``  first-match-wins. One rule fires, or none. A nested chain of\n                  conditionals in hand-written code flattens into a flat list of\n                  guarded rules without changing which branch runs, which is\n                  what lets an existing policy be re-expressed exactly.\n``select_all``    every matching rule fires, in declaration order. For groups\n                  whose members are independent decisions rather than\n                  alternatives, so several can happen on one turn.\n\nEmit operands are evaluated at selection time, in the same environment as the\nguard, so a rule cannot observe a register write made after it fired.\n"""\n\nfrom __future__ import annotations\n\nfrom collections.abc import Iterable, Sequence\nfrom dataclasses import dataclass\n\nfrom .expr import Env, Expr, Value, evaluate\n\n\n@dataclass(frozen=True)\nclass Emit:\n    """An action head plus unevaluated operand expressions."""\n\n    op: str\n    operands: tuple[Expr, ...]\n\n\n@dataclass(frozen=True)\nclass Rule:\n    name: str\n    when: Expr\n    emit: Emit\n\n\n@dataclass(frozen=True)\nclass Firing:\n    """A rule that matched, with its operands already evaluated."""\n\n    rule: str\n    op: str\n    operands: tuple[Value, ...]\n\n\ndef select_first(rules: Sequence[Rule], env: Env) -> Firing | None:\n    """Return the first matching rule\'s firing, or None if nothing matched."""\n    for rule in rules:\n        if evaluate(rule.when, env):\n            return _fire(rule, env)\n    return None\n\n\ndef select_all(rules: Sequence[Rule], env: Env) -> tuple[Firing, ...]:\n    """Return a firing for every matching rule, in declaration order."""\n    return tuple(_fire(rule, env) for rule in rules if evaluate(rule.when, env))\n\n\ndef fired_names(firings: Iterable[Firing]) -> tuple[str, ...]:\n    """Rule names in firing order, as the commit stage\'s ``fired`` leaves see them."""\n    return tuple(firing.rule for firing in firings)\n\n\ndef _fire(rule: Rule, env: Env) -> Firing:\n    return Firing(\n        rule=rule.name,\n        op=rule.emit.op,\n        operands=tuple(evaluate(operand, env) for operand in rule.emit.operands),\n    )\n',
    'dsl.pipeline': '"""Role 3 of the policy DSL: the staged register machine.\n\nThe four stages are reset, observe, decide, and commit. Within a stage every\nright-hand side is evaluated against the register values as they stood at the\n*start* of that stage, and all writes land together. This is Verilog\'s\nnon-blocking assignment (``<=``), and it exists here for the same reason it\nexists there: it removes any question of whether a register had already been\nupdated when another expression read it.\n\n``["next", reg]`` is the one deliberate exception. It reads a register written\nearlier in the same stage\'s declaration order, and it is what lets\n``mode_entered_step`` notice the mode transition that the write above it made.\n\nThe decide stage is not modelled here; it produces actions rather than register\nwrites, and the interpreter runs it between ``run_writes`` calls. What this\nmodule fixes is the meaning of ``state`` per stage: in ``observe`` it is the\nvalue carried in from the previous turn, and in ``decide`` and ``commit`` it is\nthe value the observe stage left behind.\n"""\n\nfrom __future__ import annotations\n\nfrom collections.abc import Callable, Mapping, Sequence\nfrom dataclasses import dataclass\nfrom typing import Literal\n\nfrom .expr import Env, Expr, Kind, Value, evaluate\n\nRegisterClass = Literal["decision", "telemetry"]\n\n\n@dataclass(frozen=True)\nclass Register:\n    """One element of the policy\'s register bank.\n\n    ``cls`` records the audit from ``docs/policy_dsl.md``: a *decision* register\n    is read by some guard and therefore enters the cross-backend semantic\n    contract, while a *telemetry* register is written but never read by a\n    decision and may diverge harmlessly. Golden vectors compare the first\n    strictly and the second loosely.\n    """\n\n    name: str\n    kind: Kind\n    init: Value\n    cls: RegisterClass\n\n\n@dataclass(frozen=True)\nclass Write:\n    reg: str\n    value: Expr\n\n\ndef initial_registers(registers: Mapping[str, Register]) -> dict[str, Value]:\n    """The register bank at episode start, and what stage 0 restores."""\n    return {name: register.init for name, register in registers.items()}\n\n\ndef run_writes(\n    writes: Sequence[Write],\n    state: Mapping[str, Value],\n    make_env: Callable[[Mapping[str, Value], Mapping[str, Value]], Env],\n) -> dict[str, Value]:\n    """Apply one stage\'s writes simultaneously and return the new register bank.\n\n    ``make_env`` is handed the frozen start-of-stage ``state`` and the growing\n    map of in-stage writes, so this module never needs to know what else an\n    environment carries.\n    """\n    written: dict[str, Value] = {}\n    for write in writes:\n        written[write.reg] = evaluate(write.value, make_env(state, written))\n    return {**state, **written}\n',
    'dsl.family': '"""Role 4 of the policy DSL: the family artifact.\n\n``load`` turns a family encoding into a validated, immutable ``Family``. Every\ncheck that can be made without an observation is made here, once, at load time:\nname resolution for parameters, registers, and observations; the stage\nrestrictions on ``next`` and ``fired``; kind agreement between each write and\nthe register it targets; emit arity and operand kinds; and enum domains.\n\nDoing all of it up front is the point. The same checks are what OCaml\'s sum\ntypes would give for free on the authoring side, and a family that loads here is\none whose evaluation cannot fail on a name or a type at turn 400.\n\nBoth game-specific vocabularies are injected: ``observations`` maps accessor\nnames to kinds, and ``emits`` maps a rule group to the action heads it may emit\nand their operand kinds. This module names no crop and no action.\n"""\n\nfrom __future__ import annotations\n\nfrom collections.abc import Mapping\nfrom dataclasses import dataclass\nfrom typing import Any\n\nfrom .cascade import Emit, Rule\nfrom .expr import (\n    BOOL,\n    INT,\n    DslError,\n    Expr,\n    Kind,\n    Obs,\n    Op,\n    Stage,\n    TypeEnv,\n    Value,\n    infer,\n    parse,\n)\nfrom .pipeline import Register, RegisterClass, Write\n\nDSL_VERSION = 1\n\nFARMER = "farmer"\nMARKET = "market"\n\n_REGISTER_CLASSES: tuple[RegisterClass, ...] = ("decision", "telemetry")\n\n_TOP_LEVEL = frozenset(\n    {\n        "$schema",\n        "policy_id",\n        "family",\n        "family_version",\n        "dsl_version",\n        "parameters",\n        "registers",\n        "reset_when",\n        "observe",\n        "market_rules",\n        "farmer_cascade",\n        "commit",\n    }\n)\n\n\n@dataclass(frozen=True)\nclass ParameterSpec:\n    """One configuration register, set before the episode and never written."""\n\n    name: str\n    kind: Kind\n    minimum: int | None = None\n    maximum: int | None = None\n\n\n@dataclass(frozen=True)\nclass Family:\n    """A parsed, name-resolved, kind-checked policy family."""\n\n    policy_id: str\n    family: str\n    family_version: int\n    dsl_version: int\n    parameters: Mapping[str, ParameterSpec]\n    registers: Mapping[str, Register]\n    reset_when: Expr\n    observe: tuple[Write, ...]\n    market_rules: tuple[Rule, ...]\n    farmer_cascade: tuple[Rule, ...]\n    commit: tuple[Write, ...]\n\n    def decision_registers(self) -> tuple[str, ...]:\n        """Registers a guard reads, and so the ones backends must agree on."""\n        return tuple(\n            name\n            for name, register in self.registers.items()\n            if register.cls == "decision"\n        )\n\n    def observation_names(self) -> frozenset[str]:\n        """Every observation this family reads, across all four stages.\n\n        Used by the interpreter to check an accessor\'s parameter dependencies\n        before the episode starts, so that a vocabulary entry needing a\n        parameter the family never declared is a load-time error rather than a\n        turn-0 ``KeyError``.\n        """\n        names: set[str] = set()\n        _collect_observations(self.reset_when, names)\n        for write in (*self.observe, *self.commit):\n            _collect_observations(write.value, names)\n        for rule in (*self.market_rules, *self.farmer_cascade):\n            _collect_observations(rule.when, names)\n            for operand in rule.emit.operands:\n                _collect_observations(operand, names)\n        return frozenset(names)\n\n    def bind(self, parameters: Mapping[str, Any]) -> dict[str, Value]:\n        """Validate a candidate\'s parameter block against the declared schema."""\n        missing = sorted(set(self.parameters) - set(parameters))\n        if missing:\n            raise DslError("parameters", f"missing: {\', \'.join(missing)}")\n        extra = sorted(set(parameters) - set(self.parameters))\n        if extra:\n            raise DslError("parameters", f"unknown: {\', \'.join(extra)}")\n\n        bound: dict[str, Value] = {}\n        for name, spec in self.parameters.items():\n            bound[name] = _check_parameter(spec, parameters[name])\n        return bound\n\n\ndef _collect_observations(expr: Expr, into: set[str]) -> None:\n    """Accumulate every ``["obs", name]`` leaf reachable from ``expr``."""\n    if isinstance(expr, Obs):\n        into.add(expr.name)\n    elif isinstance(expr, Op):\n        for arg in expr.args:\n            _collect_observations(arg, into)\n\n\ndef load(\n    document: Mapping[str, Any],\n    *,\n    observations: Mapping[str, Kind],\n    emits: Mapping[str, Mapping[str, tuple[Kind, ...]]],\n) -> Family:\n    """Parse and fully validate a family encoding."""\n    unknown = sorted(set(document) - _TOP_LEVEL)\n    if unknown:\n        raise DslError("family", f"unknown top-level keys: {\', \'.join(unknown)}")\n\n    dsl_version = _require(document, "dsl_version", int, "family")\n    if dsl_version != DSL_VERSION:\n        raise DslError(\n            "family.dsl_version",\n            f"this interpreter implements dsl_version {DSL_VERSION}, "\n            f"the encoding declares {dsl_version}",\n        )\n\n    parameters = _load_parameters(document)\n    registers = _load_registers(document)\n\n    groups = {\n        FARMER: _rule_names(document, "farmer_cascade"),\n        MARKET: _rule_names(document, "market_rules"),\n    }\n    param_kinds = {name: spec.kind for name, spec in parameters.items()}\n    register_kinds = {name: reg.kind for name, reg in registers.items()}\n\n    def type_env(\n        stage: Stage, next_available: frozenset[str] = frozenset()\n    ) -> TypeEnv:\n        return TypeEnv(\n            params=param_kinds,\n            registers=register_kinds,\n            observations=observations,\n            stage=stage,\n            next_available=next_available,\n            groups=groups,\n        )\n\n    reset_when = parse(_require_key(document, "reset_when", "family"), "reset_when")\n    if infer(reset_when, type_env("reset"), "reset_when").base != "bool":\n        raise DslError("reset_when", "must be a boolean expression")\n\n    observe = _load_writes(document, "observe", registers, type_env)\n    commit = _load_writes(document, "commit", registers, type_env)\n\n    farmer_cascade = _load_rules(\n        document, "farmer_cascade", FARMER, emits, type_env("decide")\n    )\n    market_rules = _load_rules(\n        document, "market_rules", MARKET, emits, type_env("decide")\n    )\n\n    return Family(\n        policy_id=_require(document, "policy_id", str, "family"),\n        family=_require(document, "family", str, "family"),\n        family_version=_require(document, "family_version", int, "family"),\n        dsl_version=dsl_version,\n        parameters=parameters,\n        registers=registers,\n        reset_when=reset_when,\n        observe=observe,\n        market_rules=market_rules,\n        farmer_cascade=farmer_cascade,\n        commit=commit,\n    )\n\n\n# --------------------------------------------------------------------------\n# Declarations\n# --------------------------------------------------------------------------\n\n\ndef _load_parameters(document: Mapping[str, Any]) -> dict[str, ParameterSpec]:\n    block = _require(document, "parameters", dict, "family")\n    specs: dict[str, ParameterSpec] = {}\n    for name, raw in block.items():\n        path = f"parameters.{name}"\n        kind = _declared_kind(raw, path)\n        if kind.base == "bool":\n            raise DslError(path, "boolean parameters are not supported")\n        minimum = _optional_int(raw, "min", path)\n        maximum = _optional_int(raw, "max", path)\n        if minimum is not None and maximum is not None and minimum > maximum:\n            raise DslError(path, f"min {minimum} exceeds max {maximum}")\n        specs[name] = ParameterSpec(name, kind, minimum, maximum)\n    return specs\n\n\ndef _load_registers(document: Mapping[str, Any]) -> dict[str, Register]:\n    block = _require(document, "registers", dict, "family")\n    registers: dict[str, Register] = {}\n    for name, raw in block.items():\n        path = f"registers.{name}"\n        kind = _declared_kind(raw, path)\n        cls = _require(raw, "class", str, path)\n        if cls not in _REGISTER_CLASSES:\n            raise DslError(\n                path, f"class must be one of {\', \'.join(_REGISTER_CLASSES)}"\n            )\n        if "init" not in raw:\n            raise DslError(path, "missing \'init\'")\n        init = _check_value(kind, raw["init"], f"{path}.init")\n        registers[name] = Register(name=name, kind=kind, init=init, cls=cls)\n    if not registers:\n        raise DslError("registers", "a family needs at least one register")\n    return registers\n\n\ndef _declared_kind(raw: Any, path: str) -> Kind:\n    if not isinstance(raw, dict):\n        raise DslError(path, f"expected an object, got {raw!r}")\n    declared = _require(raw, "type", str, path)\n    if declared == "int":\n        return INT\n    if declared == "bool":\n        return BOOL\n    if declared == "enum":\n        values = _require(raw, "values", list, path)\n        if not values or not all(isinstance(v, str) for v in values):\n            raise DslError(path, "enum \'values\' must be a non-empty list of strings")\n        return Kind("str", frozenset(values))\n    raise DslError(\n        path, f"unknown type \'{declared}\'; the DSL has int, bool, and enum only"\n    )\n\n\n# --------------------------------------------------------------------------\n# Stages\n# --------------------------------------------------------------------------\n\n\ndef _load_writes(\n    document: Mapping[str, Any],\n    key: str,\n    registers: Mapping[str, Register],\n    type_env: Any,\n) -> tuple[Write, ...]:\n    block = _require(document, key, list, "family")\n    stage: Stage = "observe" if key == "observe" else "commit"\n    writes: list[Write] = []\n    written: set[str] = set()\n    for index, raw in enumerate(block):\n        path = f"{key}[{index}]"\n        if not isinstance(raw, dict) or set(raw) != {"reg", "value"}:\n            raise DslError(path, "expected an object with exactly \'reg\' and \'value\'")\n        name = raw["reg"]\n        if name not in registers:\n            raise DslError(path, f"unknown register \'{name}\'")\n        if name in written:\n            raise DslError(\n                path,\n                f"\'{name}\' is written twice in the {stage} stage; writes within a "\n                "stage commit simultaneously, so a second write would be ambiguous",\n            )\n        value = parse(raw["value"], f"{path}.value")\n        kind = infer(value, type_env(stage, frozenset(written)), f"{path}.value")\n        _check_assignable(registers[name], kind, f"{path}.value")\n        written.add(name)\n        writes.append(Write(reg=name, value=value))\n    return tuple(writes)\n\n\ndef _load_rules(\n    document: Mapping[str, Any],\n    key: str,\n    group: str,\n    emits: Mapping[str, Mapping[str, tuple[Kind, ...]]],\n    tenv: TypeEnv,\n) -> tuple[Rule, ...]:\n    block = _require(document, key, list, "family")\n    if group not in emits:\n        raise DslError(key, f"no emit vocabulary supplied for group \'{group}\'")\n    signatures = emits[group]\n    rules: list[Rule] = []\n    seen: set[str] = set()\n    for index, raw in enumerate(block):\n        path = f"{key}[{index}]"\n        if not isinstance(raw, dict) or set(raw) != {"name", "when", "emit"}:\n            raise DslError(\n                path, "expected an object with exactly \'name\', \'when\', and \'emit\'"\n            )\n        name = raw["name"]\n        if not isinstance(name, str) or not name:\n            raise DslError(path, "\'name\' must be a non-empty string")\n        if name in seen:\n            raise DslError(path, f"duplicate rule name \'{name}\'")\n        seen.add(name)\n\n        when = parse(raw["when"], f"{path}.when")\n        if infer(when, tenv, f"{path}.when").base != "bool":\n            raise DslError(f"{path}.when", "a guard must be a boolean expression")\n\n        emit = _load_emit(raw["emit"], signatures, tenv, path)\n        rules.append(Rule(name=name, when=when, emit=emit))\n    return tuple(rules)\n\n\ndef _load_emit(\n    raw: Any,\n    signatures: Mapping[str, tuple[Kind, ...]],\n    tenv: TypeEnv,\n    path: str,\n) -> Emit:\n    if not isinstance(raw, list) or not raw or not isinstance(raw[0], str):\n        raise DslError(f"{path}.emit", f"expected [op, ...operands], got {raw!r}")\n    op = raw[0]\n    if op not in signatures:\n        known = ", ".join(sorted(signatures)) or "<none>"\n        raise DslError(f"{path}.emit", f"unknown action \'{op}\'. Available: {known}")\n    expected = signatures[op]\n    operands = raw[1:]\n    if len(operands) != len(expected):\n        raise DslError(\n            f"{path}.emit",\n            f"\'{op}\' takes {len(expected)} operand(s), got {len(operands)}",\n        )\n    parsed: list[Expr] = []\n    for index, (operand, want) in enumerate(zip(operands, expected, strict=True)):\n        where = f"{path}.emit[{index + 1}]"\n        expression = parse(operand, where)\n        got = infer(expression, tenv, where)\n        if got.base != want.base:\n            raise DslError(where, f"\'{op}\' operand {index} must be {want}, got {got}")\n        parsed.append(expression)\n    return Emit(op=op, operands=tuple(parsed))\n\n\ndef _rule_names(document: Mapping[str, Any], key: str) -> frozenset[str]:\n    block = document.get(key)\n    if not isinstance(block, list):\n        raise DslError(f"family.{key}", "expected an array of rules")\n    return frozenset(\n        raw["name"]\n        for raw in block\n        if isinstance(raw, dict) and isinstance(raw.get("name"), str)\n    )\n\n\n# --------------------------------------------------------------------------\n# Value checking\n# --------------------------------------------------------------------------\n\n\ndef _check_assignable(register: Register, kind: Kind, path: str) -> None:\n    if kind.base != register.kind.base:\n        raise DslError(\n            path,\n            f"cannot write {kind} to register \'{register.name}\' of type "\n            f"{register.kind}",\n        )\n    if register.kind.values is not None and kind.values is not None:\n        stray = sorted(kind.values - register.kind.values)\n        if stray:\n            raise DslError(\n                path,\n                f"writes value(s) {\', \'.join(stray)} outside the declared domain "\n                f"of \'{register.name}\'",\n            )\n\n\ndef _check_value(kind: Kind, value: Any, path: str) -> Value:\n    if kind.base == "bool":\n        if not isinstance(value, bool):\n            raise DslError(path, f"expected a boolean, got {value!r}")\n        return value\n    if kind.base == "int":\n        if isinstance(value, bool) or not isinstance(value, int):\n            raise DslError(path, f"expected an integer, got {value!r}")\n        return value\n    if not isinstance(value, str):\n        raise DslError(path, f"expected a string, got {value!r}")\n    if kind.values is not None and value not in kind.values:\n        raise DslError(\n            path, f"\'{value}\' is not one of {\', \'.join(sorted(kind.values))}"\n        )\n    return value\n\n\ndef _check_parameter(spec: ParameterSpec, value: Any) -> Value:\n    path = f"parameters.{spec.name}"\n    checked = _check_value(spec.kind, value, path)\n    if spec.kind.base == "int":\n        assert isinstance(checked, int)\n        if spec.minimum is not None and checked < spec.minimum:\n            raise DslError(path, f"{checked} is below the minimum {spec.minimum}")\n        if spec.maximum is not None and checked > spec.maximum:\n            raise DslError(path, f"{checked} is above the maximum {spec.maximum}")\n    return checked\n\n\n# --------------------------------------------------------------------------\n# Small helpers\n# --------------------------------------------------------------------------\n\n\ndef _require_key(document: Mapping[str, Any], key: str, path: str) -> Any:\n    if key not in document:\n        raise DslError(path, f"missing \'{key}\'")\n    return document[key]\n\n\ndef _require(document: Mapping[str, Any], key: str, want: type, path: str) -> Any:\n    value = _require_key(document, key, path)\n    if want is int and isinstance(value, bool):\n        raise DslError(f"{path}.{key}", f"expected an integer, got {value!r}")\n    if not isinstance(value, want):\n        raise DslError(\n            f"{path}.{key}", f"expected {want.__name__}, got {type(value).__name__}"\n        )\n    return value\n\n\ndef _optional_int(raw: Mapping[str, Any], key: str, path: str) -> int | None:\n    if key not in raw:\n        return None\n    return _require(raw, key, int, path)\n\n\n__all__ = [\n    "DSL_VERSION",\n    "FARMER",\n    "MARKET",\n    "Family",\n    "ParameterSpec",\n    "load",\n]\n',
    'dsl.interpreter': '"""Binds the four DSL roles to an injected, game-specific vocabulary.\n\nThis is the module ``submission/main.py`` ultimately vendors. It holds the turn\nloop and nothing else: the language is in ``expr``, the selection disciplines\nare in ``cascade``, the stage semantics are in ``pipeline``, and the artifact is\nin ``family``. What Kaggriculture knowledge the running policy needs arrives\nthrough the two injected seams, ``Vocabulary`` and ``build_action``, so that\nthis file can be read without knowing what a crop is.\n\nObservation accessors are resolved lazily and memoised for the duration of one\nturn. Laziness is load-bearing rather than an optimisation: ``and`` short-\ncircuits, so a guard such as ``harvest_ready`` never asks for ``tile_planted_day``\non a tile that is not a plant.\n"""\n\nfrom __future__ import annotations\n\nfrom collections.abc import Callable, Mapping\nfrom dataclasses import dataclass, field\nfrom typing import Any\n\nfrom .cascade import Firing, fired_names, select_all, select_first\nfrom .expr import DslError, Env, Kind, Value, evaluate\nfrom .family import FARMER, MARKET, Family\nfrom .pipeline import initial_registers, run_writes\n\n\n@dataclass(frozen=True)\nclass Vocabulary:\n    """The per-game observation seam.\n\n    ``kinds`` is what ``family.load`` type-checks against; ``accessors`` is what\n    the turn loop calls. They are separate fields because the first is needed\n    before any observation exists and the second only afterwards, and because\n    keeping the declaration next to the implementation is what stops the two\n    drifting apart.\n\n    ``requires`` records the third thing a caller cannot otherwise see: which\n    *parameters* an accessor reads. An accessor that resolves an item through\n    ``parameters["crop"]`` is undefined for a family that declares no such\n    parameter, and nothing in ``kinds`` says so. Listing the dependency here is\n    what lets that be caught before the episode starts rather than as a\n    ``KeyError`` on turn 0. Accessors that read no parameter are simply absent.\n    """\n\n    kinds: Mapping[str, Kind]\n    accessors: Mapping[str, Callable[[Any, Mapping[str, Value]], Value]]\n    requires: Mapping[str, tuple[str, ...]] = field(default_factory=dict)\n\n    def __post_init__(self) -> None:\n        undeclared = sorted(set(self.accessors) - set(self.kinds))\n        unimplemented = sorted(set(self.kinds) - set(self.accessors))\n        if undeclared or unimplemented:\n            raise DslError(\n                "vocabulary",\n                f"declared-but-missing: {unimplemented or \'<none>\'}; "\n                f"implemented-but-undeclared: {undeclared or \'<none>\'}",\n            )\n        stray = sorted(set(self.requires) - set(self.kinds))\n        if stray:\n            raise DslError(\n                "vocabulary.requires",\n                f"names no accessor declares: {\', \'.join(stray)}",\n            )\n\n\n@dataclass(frozen=True)\nclass Turn:\n    """What the decide stage produced, before it is shaped into a game action."""\n\n    farmer: Firing | None\n    market: tuple[Firing, ...]\n\n\nActionBuilder = Callable[[Turn], Any]\n\n\nclass Interpreter:\n    """A family plus bound parameters: pure, stateless, reusable across players."""\n\n    def __init__(\n        self,\n        family: Family,\n        parameters: Mapping[str, Value],\n        vocabulary: Vocabulary,\n        build_action: ActionBuilder,\n    ) -> None:\n        missing = sorted(set(family.parameters) - set(parameters))\n        if missing:\n            raise DslError("interpreter", f"unbound parameters: {\', \'.join(missing)}")\n\n        # Every accessor this family actually reads must have the parameters it\n        # depends on. Checked here because this is the first point at which the\n        # family, the vocabulary, and the bound parameters are all in hand.\n        undeclared: dict[str, list[str]] = {}\n        for observation in sorted(family.observation_names()):\n            for name in vocabulary.requires.get(observation, ()):\n                if name not in parameters:\n                    undeclared.setdefault(name, []).append(observation)\n        if undeclared:\n            detail = "; ".join(\n                f"\'{name}\' (needed by {\', \'.join(readers)})"\n                for name, readers in sorted(undeclared.items())\n            )\n            raise DslError(\n                "interpreter",\n                f"family declares no parameter {detail}",\n            )\n\n        self.family = family\n        self.parameters = dict(parameters)\n        self.vocabulary = vocabulary\n        self.build_action = build_action\n\n    def initial_registers(self) -> dict[str, Value]:\n        return initial_registers(self.family.registers)\n\n    def step(\n        self, observation: Any, registers: Mapping[str, Value]\n    ) -> tuple[Any, dict[str, Value]]:\n        """Run one turn: ``(observation, registers) -> (action, next registers)``."""\n        observe = self._resolver(observation)\n        state = dict(registers)\n\n        # Stage 0 - reset. Restoring every init reproduces the hand-written\n        # policy\'s habit of constructing a fresh PolicyState.\n        if evaluate(self.family.reset_when, Env(self.parameters, state, observe)):\n            state = self.initial_registers()\n\n        # Stage 1 - observe. Register writes that decisions are allowed to see.\n        state = run_writes(\n            self.family.observe,\n            state,\n            lambda base, written: Env(self.parameters, base, observe, next=written),\n        )\n\n        # Stage 2 - decide. Reads the values stage 1 left behind.\n        decide = Env(self.parameters, state, observe)\n        market = select_all(self.family.market_rules, decide)\n        farmer = select_first(self.family.farmer_cascade, decide)\n\n        # Stage 3 - commit. May ask which rules fired.\n        groups = {\n            FARMER: () if farmer is None else (farmer.rule,),\n            MARKET: fired_names(market),\n        }\n        state = run_writes(\n            self.family.commit,\n            state,\n            lambda base, written: Env(\n                self.parameters, base, observe, next=written, groups=groups\n            ),\n        )\n\n        return self.build_action(Turn(farmer=farmer, market=market)), state\n\n    def _resolver(self, observation: Any) -> Callable[[str], Value]:\n        cache: dict[str, Value] = {}\n        accessors = self.vocabulary.accessors\n        kinds = self.vocabulary.kinds\n        parameters = self.parameters\n\n        def resolve(name: str) -> Value:\n            if name not in cache:\n                value = accessors[name](observation, parameters)\n                cache[name] = _check_observed(name, kinds[name], value)\n            return cache[name]\n\n        return resolve\n\n\nclass Policy:\n    """One interpreter plus one player\'s register bank, shaped like a policy."""\n\n    def __init__(self, interpreter: Interpreter) -> None:\n        self.interpreter = interpreter\n        self.registers = interpreter.initial_registers()\n\n    def reset(self) -> None:\n        self.registers = self.interpreter.initial_registers()\n\n    def act(self, observation: Any) -> Any:\n        action, self.registers = self.interpreter.step(observation, self.registers)\n        return action\n\n    def snapshot(self) -> dict[str, Value]:\n        """Final register values, in declaration order, for reports and fixtures."""\n        return dict(self.registers)\n\n    def decision_snapshot(self) -> dict[str, Value]:\n        """Only the registers a guard reads, which is what backends must match."""\n        return {\n            name: self.registers[name]\n            for name in self.interpreter.family.decision_registers()\n        }\n\n\ndef _check_observed(name: str, kind: Kind, value: Any) -> Value:\n    """Type-check a value crossing in from the game.\n\n    The only runtime type check in the interpreter, and it belongs exactly here:\n    everything inside has been checked statically by ``family.load``, while an\n    accessor reads dictionaries the game handed us.\n    """\n    if kind.base == "bool":\n        if not isinstance(value, bool):\n            raise DslError(f"obs.{name}", f"expected a boolean, got {value!r}")\n        return value\n    if kind.base == "int":\n        if isinstance(value, bool) or not isinstance(value, int):\n            raise DslError(f"obs.{name}", f"expected an integer, got {value!r}")\n        return value\n    if not isinstance(value, str):\n        raise DslError(f"obs.{name}", f"expected a string, got {value!r}")\n    if kind.values is not None and value not in kind.values:\n        raise DslError(\n            f"obs.{name}", f"\'{value}\' is not one of {\', \'.join(sorted(kind.values))}"\n        )\n    return value\n',
    'actions': '"""The Kaggriculture action seam: what a rule may emit, and how it is shaped.\n\n``EMITS`` is the static half, consumed by ``family.load`` so that a misspelled\n``["SEL", ...]`` or a ``["PLANT"]`` missing its crop fails at load rather than\nmid-episode. ``build_action`` is the runtime half, turning the decide stage\'s\nfirings into the list-encoded action the environment expects.\n\nLike ``vocabulary.py`` this file names crops and actions, and like that file it\nis deliberately separable from ``submission/dsl/`` by deletion.\n"""\n\nfrom __future__ import annotations\n\nfrom typing import Any, TypedDict\n\nfrom _kaggriculture_submission.dsl.expr import INT, STR, Kind\nfrom _kaggriculture_submission.dsl.family import FARMER, MARKET\nfrom _kaggriculture_submission.dsl.interpreter import Turn\n\nWorkerAction = list[Any]\nMarketOrder = list[Any]\n\n\nclass PolicyAction(TypedDict):\n    """The complete action returned by a policy for one turn."""\n\n    farmer: WorkerAction\n    hands: list[WorkerAction]\n    market: list[MarketOrder]\n\n\nWORKER_EMITS: dict[str, tuple[Kind, ...]] = {\n    "PASS": (),\n    "DROP": (),\n    "HARVEST": (),\n    "WATER": (),\n    "PLANT": (STR,),\n}\n\nMARKET_EMITS: dict[str, tuple[Kind, ...]] = {\n    "BUY_SEED": (STR, INT),\n    "SELL": (STR, INT),\n}\n\nEMITS: dict[str, dict[str, tuple[Kind, ...]]] = {\n    FARMER: WORKER_EMITS,\n    MARKET: MARKET_EMITS,\n}\n\n# Market orders carry a unit count as their last operand; worker actions do not.\n_UNIT_OPERAND = -1\n\n\ndef build_action(turn: Turn) -> PolicyAction:\n    """Shape one turn\'s firings into the environment\'s action encoding.\n\n    A farmer cascade with no matching rule passes, which is the same default the\n    hand-written policy falls through to. A well-formed family ends its cascade\n    with a catch-all, so this is a safety net rather than a normal path.\n    """\n    farmer: WorkerAction = (\n        ["PASS"]\n        if turn.farmer is None\n        else [turn.farmer.op, *turn.farmer.operands]\n    )\n    market: list[MarketOrder] = []\n    for firing in turn.market:\n        units = firing.operands[_UNIT_OPERAND]\n        if not isinstance(units, int) or isinstance(units, bool) or units < 1:\n            raise ValueError(\n                f"market rule \'{firing.rule}\' emitted {units!r} units; "\n                "action units must be a positive integer"\n            )\n        market.append([firing.op, *firing.operands])\n    return {"farmer": farmer, "hands": [], "market": market}\n',
    'vocabulary': '"""The Kaggriculture observation vocabulary: the DSL\'s only game-aware seam.\n\n``docs/policy_dsl.md`` is blunt about where the cost of a data-defined family\nactually lives: not in the rules, which are free, but in the accessors. Adding\n``opponent_money`` costs one line in every backend. Adding a guard that uses it\ncosts nothing. So this table is the file that gets ported, and everything under\n``submission/dsl/`` is the part that does not.\n\nIt duplicates ``experiments/policies/common/observations.py`` and the\n``SEED_COSTS`` table from ``common/game_data.py`` on purpose. The submission is\nstdlib-only and self-contained, so it cannot import them; ``tests/test_dsl_\nvocabulary.py`` asserts the two copies still agree rather than trusting them to.\n\nAccessors take ``(observation, parameters)`` because several of them resolve\n``param.crop`` at evaluation time, which is what keeps the family crop-generic\neven while v1 pins the crop to WHEAT.\n"""\n\nfrom __future__ import annotations\n\nfrom collections.abc import Mapping\nfrom typing import Any\n\nfrom _kaggriculture_submission.dsl.expr import BOOL, INT, Kind, Value\nfrom _kaggriculture_submission.dsl.interpreter import Vocabulary\n\n# Official fixed seed prices. Mirrors experiments/policies/common/game_data.py.\nSEED_COSTS: Mapping[str, int] = {\n    "WHEAT": 10,\n    "CARROT": 20,\n    "TOMATO": 50,\n    "STRAWBERRY": 100,\n    "MELON": 80,\n}\n\n\nclass ObservationError(ValueError):\n    """A game observation that does not match what the vocabulary expects."""\n\n\n# --------------------------------------------------------------------------\n# Shared readers\n# --------------------------------------------------------------------------\n\n\ndef _own_farm(observation: Mapping[str, Any]) -> Mapping[str, Any]:\n    return observation["farms"][int(observation["player"])]\n\n\ndef _item_count(container: Mapping[str, Any] | None, item: str) -> int:\n    if container is None:\n        return 0\n    return max(0, int(container.get(item, 0)))\n\n\ndef _crop(parameters: Mapping[str, Value]) -> str:\n    crop = parameters["crop"]\n    assert isinstance(crop, str)\n    return crop\n\n\ndef _current_tile(farm: Mapping[str, Any]) -> Any:\n    x, y = farm["farmer"]\n    return farm["tiles"][int(y)][int(x)]\n\n\ndef _plant_tile(observation: Mapping[str, Any]) -> Mapping[str, Any] | None:\n    tile = _current_tile(_own_farm(observation))\n    if isinstance(tile, Mapping) and tile.get("kind") == "PLANT":\n        return tile\n    return None\n\n\ndef _worker_inventory(\n    private: Mapping[str, Any], worker_index: int = 0\n) -> Mapping[str, Any]:\n    inventories = private.get("inventories") or []\n    if not 0 <= worker_index < len(inventories):\n        return {}\n    inventory = inventories[worker_index]\n    return inventory if isinstance(inventory, Mapping) else {}\n\n\ndef _integral(value: Any, name: str) -> int:\n    """Convert an integer-valued float to int, refusing to round.\n\n    ``money`` is declared ``float`` in the observation schema but is integral by\n    construction upstream: it starts at ``float(startingMoney)`` and every delta\n    is an integer price, cost, or hire fee. Exposing it to the DSL as an ``int``\n    is therefore exact, not an approximation, and it keeps floats out of a\n    language that deliberately has no float arithmetic.\n\n    If that ever stops being true, this raises rather than silently diverging\n    from the hand-written policy\'s float comparison.\n    """\n    number = float(value)\n    if number != int(number):\n        raise ObservationError(\n            f"{name} is {number!r}, which is not an integer; the DSL has no float "\n            "arithmetic and money was expected to be integral"\n        )\n    return int(number)\n\n\n# --------------------------------------------------------------------------\n# Accessors\n# --------------------------------------------------------------------------\n\n\ndef _step(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    return int(observation["step"])\n\n\ndef _day(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    return int(observation["day"])\n\n\ndef _hour(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    return int(observation["hour"])\n\n\ndef _money(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    return _integral(_own_farm(observation)["money"], "money")\n\n\ndef _seeds(observation: Mapping[str, Any], parameters: Mapping[str, Value]) -> int:\n    return _item_count(observation["private"].get("seeds"), _crop(parameters))\n\n\ndef _shed_units(\n    observation: Mapping[str, Any], parameters: Mapping[str, Value]\n) -> int:\n    return _item_count(observation["private"].get("shed"), _crop(parameters))\n\n\ndef _market_price(\n    observation: Mapping[str, Any], parameters: Mapping[str, Value]\n) -> int:\n    return _item_count(observation["market"].get("prices"), _crop(parameters))\n\n\ndef _seed_cost(_: Mapping[str, Any], parameters: Mapping[str, Value]) -> int:\n    crop = _crop(parameters)\n    try:\n        return SEED_COSTS[crop]\n    except KeyError:\n        raise ObservationError(f"unknown crop: {crop}") from None\n\n\ndef _carried_units(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    inventory = _worker_inventory(observation["private"])\n    return sum(max(0, int(count)) for count in inventory.values())\n\n\ndef _on_shed_access(observation: Mapping[str, Any], _: Mapping[str, Value]) -> bool:\n    farm = _own_farm(observation)\n    tiles = farm["tiles"]\n    height = len(tiles)\n    width = len(tiles[0]) if height else 0\n    if width < 2 or height < 2:\n        return False\n    x, y = farm["farmer"]\n    middle_x = width // 2\n    middle_y = height // 2\n    return (int(x), int(y)) in {\n        (middle_x - 1, middle_y - 1),\n        (middle_x, middle_y - 1),\n        (middle_x - 1, middle_y),\n        (middle_x, middle_y),\n    }\n\n\ndef _tile_is_empty(observation: Mapping[str, Any], _: Mapping[str, Value]) -> bool:\n    return _current_tile(_own_farm(observation)) is None\n\n\ndef _tile_is_plant(observation: Mapping[str, Any], _: Mapping[str, Value]) -> bool:\n    return _plant_tile(observation) is not None\n\n\ndef _tile_planted_day(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    tile = _plant_tile(observation)\n    return -1 if tile is None else int(tile["planted_day"])\n\n\ndef _tile_yield_units(observation: Mapping[str, Any], _: Mapping[str, Value]) -> int:\n    tile = _plant_tile(observation)\n    return 0 if tile is None else int(tile.get("yield_units", 0))\n\n\ndef _tile_watered_today(\n    observation: Mapping[str, Any], _: Mapping[str, Value]\n) -> bool:\n    tile = _plant_tile(observation)\n    return False if tile is None else bool(tile.get("watered_today", False))\n\n\nKINDS: Mapping[str, Kind] = {\n    "step": INT,\n    "day": INT,\n    "hour": INT,\n    "money": INT,\n    "seeds": INT,\n    "shed_units": INT,\n    "market_price": INT,\n    "seed_cost": INT,\n    "carried_units": INT,\n    "on_shed_access": BOOL,\n    "tile_is_empty": BOOL,\n    "tile_is_plant": BOOL,\n    "tile_planted_day": INT,\n    "tile_yield_units": INT,\n    "tile_watered_today": BOOL,\n}\n\nACCESSORS = {\n    "step": _step,\n    "day": _day,\n    "hour": _hour,\n    "money": _money,\n    "seeds": _seeds,\n    "shed_units": _shed_units,\n    "market_price": _market_price,\n    "seed_cost": _seed_cost,\n    "carried_units": _carried_units,\n    "on_shed_access": _on_shed_access,\n    "tile_is_empty": _tile_is_empty,\n    "tile_is_plant": _tile_is_plant,\n    "tile_planted_day": _tile_planted_day,\n    "tile_yield_units": _tile_yield_units,\n    "tile_watered_today": _tile_watered_today,\n}\n\n# Which parameters each accessor reads. Four of them resolve an item through\n# ``parameters["crop"]``, so a family that reads one but declares no ``crop``\n# parameter is not merely unusual -- it is undefined, and used to surface as a\n# KeyError on turn 0 rather than as a load failure. Accessors reading no\n# parameter are absent rather than mapped to an empty tuple.\n#\n# Mirrored on the authoring side by ``requires`` in\n# ``authoring/kaggriculture/vocabulary.ml``; the check itself lives in\n# ``Interpreter.__init__``.\nREQUIRES: Mapping[str, tuple[str, ...]] = {\n    "seeds": ("crop",),\n    "shed_units": ("crop",),\n    "market_price": ("crop",),\n    "seed_cost": ("crop",),\n}\n\nVOCABULARY = Vocabulary(kinds=KINDS, accessors=ACCESSORS, requires=REQUIRES)\n',
}


def _install_package(name):
    package = _types.ModuleType(name)
    package.__file__ = _ARTIFACT_FILE
    package.__package__ = name
    package.__path__ = []
    _sys.modules[name] = package
    if "." in name:
        parent_name, child_name = name.rsplit(".", 1)
        setattr(_sys.modules[parent_name], child_name, package)
    return package


def _install_module(name, source):
    module = _types.ModuleType(name)
    module.__file__ = _ARTIFACT_FILE + ":" + name
    module.__package__ = name.rpartition(".")[0]
    _sys.modules[name] = module
    parent_name, child_name = name.rsplit(".", 1)
    setattr(_sys.modules[parent_name], child_name, module)
    exec(compile(source, module.__file__, "exec"), module.__dict__)
    return module


_install_package(_RUNTIME_PREFIX)
_install_package(_RUNTIME_PREFIX + ".dsl")
for _suffix, _source in _MODULE_SOURCES.items():
    _install_module(_RUNTIME_PREFIX + "." + _suffix, _source)

_actions = _sys.modules[_RUNTIME_PREFIX + ".actions"]
_family_module = _sys.modules[_RUNTIME_PREFIX + ".dsl.family"]
_interpreter_module = _sys.modules[_RUNTIME_PREFIX + ".dsl.interpreter"]
_vocabulary = _sys.modules[_RUNTIME_PREFIX + ".vocabulary"]

_FAMILY_DOCUMENT = _json.loads('{"policy_id":"monocrop-reorder-v1","family":"monocrop_reorder","family_version":1,"dsl_version":1,"parameters":{"crop":{"type":"enum","values":["WHEAT"]},"cash_reserve":{"type":"int","min":0},"seed_reorder_point":{"type":"int","min":0},"seed_buy_batch":{"type":"int","min":1},"planting_hour_cutoff":{"type":"int","min":0,"max":22},"harvest_min_age_days":{"type":"int","min":2,"max":5},"sell_price_threshold":{"type":"int","min":1},"liquidation_start_day":{"type":"int","min":0,"max":29}},"registers":{"mode":{"type":"enum","values":["OPENING","PRODUCTION","LIQUIDATION"],"init":"OPENING","class":"decision"},"last_step":{"type":"int","init":-1,"class":"decision"},"requested_plant_actions":{"type":"int","init":0,"class":"decision"},"mode_entered_step":{"type":"int","init":0,"class":"telemetry"},"money_seen":{"type":"bool","init":false,"class":"telemetry"},"previous_money":{"type":"int","init":0,"class":"telemetry"},"last_money_delta":{"type":"int","init":0,"class":"telemetry"},"peak_price":{"type":"int","init":0,"class":"telemetry"},"requested_harvest_actions":{"type":"int","init":0,"class":"telemetry"},"requested_sell_units":{"type":"int","init":0,"class":"telemetry"}},"reset_when":["and",["==",["obs","step"],["const",0]],[">=",["state","last_step"],["const",0]]],"observe":[{"reg":"last_money_delta","value":["if",["state","money_seen"],["-",["obs","money"],["state","previous_money"]],["const",0]]},{"reg":"previous_money","value":["obs","money"]},{"reg":"money_seen","value":["const",true]},{"reg":"peak_price","value":["max",["state","peak_price"],["obs","market_price"]]},{"reg":"mode","value":["if",[">=",["obs","day"],["param","liquidation_start_day"]],["const","LIQUIDATION"],["if",[">",["state","requested_plant_actions"],["const",0]],["const","PRODUCTION"],["state","mode"]]]},{"reg":"mode_entered_step","value":["if",["!=",["next","mode"],["state","mode"]],["obs","step"],["state","mode_entered_step"]]}],"market_rules":[{"name":"sell_stock","when":["and",[">",["obs","shed_units"],["const",0]],["or",["==",["state","mode"],["const","LIQUIDATION"]],[">=",["obs","market_price"],["param","sell_price_threshold"]]]],"emit":["SELL",["param","crop"],["obs","shed_units"]]},{"name":"reorder_seeds","when":["and",["!=",["state","mode"],["const","LIQUIDATION"]],["<=",["obs","seeds"],["param","seed_reorder_point"]],[">=",["-",["obs","money"],["*",["param","seed_buy_batch"],["obs","seed_cost"]]],["param","cash_reserve"]]],"emit":["BUY_SEED",["param","crop"],["param","seed_buy_batch"]]}],"farmer_cascade":[{"name":"stow_carried","when":["and",[">",["obs","carried_units"],["const",0]],["obs","on_shed_access"]],"emit":["DROP"]},{"name":"harvest_ready","when":["and",["obs","tile_is_plant"],[">=",["-",["obs","day"],["obs","tile_planted_day"]],["param","harvest_min_age_days"]],[">",["obs","tile_yield_units"],["const",0]]],"emit":["HARVEST"]},{"name":"water_crop","when":["and",["obs","tile_is_plant"],["not",["obs","tile_watered_today"]]],"emit":["WATER"]},{"name":"tend_wait","when":["obs","tile_is_plant"],"emit":["PASS"]},{"name":"plant_seed","when":["and",["obs","tile_is_empty"],[">",["obs","seeds"],["const",0]],["<=",["obs","hour"],["param","planting_hour_cutoff"]],["!=",["state","mode"],["const","LIQUIDATION"]]],"emit":["PLANT",["param","crop"]]},{"name":"idle","when":["const",true],"emit":["PASS"]}],"commit":[{"reg":"requested_plant_actions","value":["+",["state","requested_plant_actions"],["if",["==",["fired","farmer"],["const","plant_seed"]],["const",1],["const",0]]]},{"reg":"requested_harvest_actions","value":["+",["state","requested_harvest_actions"],["if",["==",["fired","farmer"],["const","harvest_ready"]],["const",1],["const",0]]]},{"reg":"requested_sell_units","value":["+",["state","requested_sell_units"],["if",["fired?","market","sell_stock"],["obs","shed_units"],["const",0]]]},{"reg":"last_step","value":["obs","step"]}]}')
_CANDIDATE_DOCUMENT = _json.loads('{"$schema":"./parameters.schema.json","policy_id":"monocrop-reorder-v1","schema_version":1,"parameters":{"crop":"WHEAT","cash_reserve":250,"seed_reorder_point":0,"seed_buy_batch":4,"planting_hour_cutoff":20,"harvest_min_age_days":4,"sell_price_threshold":25,"liquidation_start_day":27}}')
if _CANDIDATE_DOCUMENT.get("policy_id") != _FAMILY_DOCUMENT.get("policy_id"):
    raise ValueError("embedded candidate and family policy_id disagree")
if _CANDIDATE_DOCUMENT.get("schema_version") != CANDIDATE_SCHEMA_VERSION:
    raise ValueError("unsupported embedded candidate schema_version")

_FAMILY = _family_module.load(
    _FAMILY_DOCUMENT,
    observations=_vocabulary.VOCABULARY.kinds,
    emits=_actions.EMITS,
)
_INTERPRETER = _interpreter_module.Interpreter(
    family=_FAMILY,
    parameters=_FAMILY.bind(POLICY_PARAMETERS),
    vocabulary=_vocabulary.VOCABULARY,
    build_action=_actions.build_action,
)
_POLICY = _interpreter_module.Policy(_INTERPRETER)


def agent(observation):
    """Select one action using the embedded family and candidate parameters."""
    if int(observation.get("step", 0)) == 0:
        _POLICY.reset()
    return _POLICY.act(observation)
